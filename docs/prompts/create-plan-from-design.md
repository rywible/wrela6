Make an implementation plan off of this design: <placeholder>. It should be able to be executed in parallel by subagents. It should be split into discrete and atomic tasks that each have description, AC, and code examples. No tasks should be spikes or deferred design. If you need to do research ahead of time for the plan, then do it and put it in the plan. The plan should be written such that any task can be picked up by a junior engineer and worked end to end to completion. Don't worry about plan length, simply write as many or as few tasks as are needed to get the job done. Don't write any actual source code. Just put it all in the plan as codeblocks.
