// Ejected ordinary project source copy; target authority comes from certified platform contracts.
platform fn set_watchdog_timer(timeout_seconds: U64) -> UefiStatus

fn disable_watchdog() -> UefiStatus:
    set_watchdog_timer(0)
